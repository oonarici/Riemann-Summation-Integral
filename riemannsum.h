#ifndef RIEMANNSUM_H
#define RIEMANNSUM_H
#include <vector>

double integrate(std::vector<double> x, std::vector<double> y, double from, double to);
unsigned long indexOf(std::vector<double> x, double element);

#endif // RIEMANNSUM_H
