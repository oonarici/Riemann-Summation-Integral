#ifndef RIEMANNSUM_H
#define RIEMANNSUM_H
#include <vector>

double integrate(std::vector<double> x, std::vector<double> y, double bound1, double bound2);
unsigned long indexOf(std::vector<double> x, double element);

#endif // RIEMANNSUM_H
